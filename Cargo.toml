[package]
name = "conmon"
version = "3.0.0-dev"
edition = "2024"
authors = ["github.com/containers"]
license = "Apache-2.0"
readme = "README.md"
description = "An OCI container runtime monitor, version 3"
homepage = "https://github.com/containers/conmon-v3"
repository = "https://github.com/containers/conmon-v3"
exclude = ["/.github/*"]

[[bin]]
name = "conmon"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", default-features = false, features = ["std", "derive", "help"] }
nix = { version = "0.30.1", features = ["process", "signal", "resource", "fs", "poll", "socket", "uio", "feature", "user"] }
serde_json = "1"
log = { version = "0.4", features = ["std"] }
chrono = "0.4"
systemd = { version = "0.10.1", default-features = false, features = ["journal"] }

[dev-dependencies]
tempfile = "3"
mockall = "0.13.1"

[profile.release]
opt-level = "z"    # Optimize for size.
codegen-units = 1  # Slower compilation, but more runtime optimizations.
